const fs=require('fs'),path=require('path');const f=path.join(__dirname,'data.json');if(!fs.existsSync(f)){console.log('No data.json found. Start the program once first.');process.exit(1)}let d=JSON.parse(fs.readFileSync(f,'utf8'));d.users=Array.isArray(d.users)?d.users:[];let u=d.users.find(x=>Number(x.id)===1)||d.users[0];if(!u){u={id:1,name:'المدير',role:'admin',active:true};d.users.push(u)}u.username='admin';u.password='123456';u.name=u.name||'المدير';u.role='admin';u.active=true;fs.writeFileSync(f,JSON.stringify(d,null,2),'utf8');console.log('Admin login reset to admin / 123456');